<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.atmcorp.taskpro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrescriptionEditActivity"></activity>
        <activity android:name=".PurchaseSingleListActivity" />
        <activity android:name=".TodoEditActivity" />
        <activity android:name=".BillPayHistoryActivity" />
        <activity android:name=".customListView"
            tools:ignore="Instantiatable" />
        <activity android:name=".PrescriptionActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".PurchaseListActivity" />
        <activity android:name=".BillsActivity" />
        <activity android:name=".BillEditActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>